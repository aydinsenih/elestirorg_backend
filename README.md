# elestirorg_backend
elestirorg

/login  method POST
Params : "username", "password"

Returns : JSON with "status".

/signup method POST
Params : "email", "username", "password", "phoneNumber"

Returns : JSON with "status".
